services:
  app:
    build: .
    ports:
      - "8985:8985"
    env_file:
      - .env.local
    volumes:
      - ./src:/app/src
    command: ["npm", "run", "dev", "--", "--port=8985"]
    restart: unless-stopped
